**NOW**

- [NOW] `h5-reader/src/app/DashboardDisplayController.cpp:1174` / `h5-reader/src/app/DashboardDisplayController.cpp:1436` / `h5-reader/src/app/DashboardStripDock.cpp:277`: static panels are moved out exactly once, then any frame tick or panel-visibility refresh emits `stripTracksChanged()` and `refreshTracks()` replaces the widget's panels with the now-empty moved-from vector. Split temporal track updates from static panel updates, or keep panel ownership in the widget and only replace it on a rebuild/static-panel generation change.

- [NOW] `h5-reader/src/app/SignalDisplayDialog.cpp:50` / `h5-reader/src/app/SignalDisplayDialog.cpp:142` / `h5-reader/src/app/SignalDisplayDialog.cpp:985`: `static.bar.sequence` is not represented by any dialog mode kind, so the Add dialog cannot select the SequenceBarPanel mode and will fall back to `static.table` for the iRED descriptor. Add a Bar/Sequence display kind, or map `static.bar.sequence` into an existing static-panel kind and preserve that exact mode id in `displayModeIds`.

- [NOW] `h5-reader/src/app/NearbySignalModel.h:29` / `h5-reader/src/app/NearbySignalModel.cpp:151` / `h5-reader/src/app/SignalDisplayDialog.cpp:236`: the BondVector axis was added to the catalog, but nearby candidates and the dialog's required-anchor filter were not extended, so selecting a residue does not surface iRED as bindable despite `AnchorMatchesAxis(Residue, BondVector)`. Add `CandidateKind::BondVector`, emit N-H rows from the iRED identity table, return `SignalAnchorKind::BondVector`, and mirror the residue-to-BondVector widening in `anchorAxisCanSatisfy()`.

- [NOW] `h5-reader/src/model/DashboardPanelModel.cpp:53` / `h5-reader/src/app/DashboardDisplayController.cpp:1388` / `h5-reader/src/app/DashboardDisplayController.cpp:1660`: `DashboardDisplayRef` generation still drops every non-`strip.*` mode, while static panels are built from all active signals without active-panel filtering. Give static panel modes a display ref, filter owned panels through the active `DashboardPanelModel`, and make the cleanup cascade account for those refs; otherwise Phase C/D panels leak across tabs and cannot be scoped or removed consistently.

- [NOW] `h5-reader/src/io/QtTrajectoryH5.cpp:741` / `h5-reader/src/io/QtTrajectoryH5.cpp:751`: `ReadIRedOrderParameters()` validates shapes but then performs unchecked HighFive reads into `std::vector<int32_t>`/`double`; an H5 dtype mismatch can throw through the reader and abort the whole trajectory load, and 64-bit residue/atom datasets can narrow silently or unpredictably. Wrap the per-group reads in a catch that leaves the buffer null, read integer identity columns into a wide signed type, validate ranges against residue/atom counts, then cast into the Qt buffer.

- [NOW] `h5-reader/src/app/SequenceBarPanel.cpp:44` / `h5-reader/src/app/SequenceBarPanel.cpp:120`: `SequenceBarPanel` keys x-position and hit-testing only by residue index, so multiple rows for one residue draw on top of each other and clicks always choose the first row. Before ReorientationalDynamics clones this panel, assign each row a distinct slot or per-kind offset and hit-test actual bar rectangles.

- [NOW] `h5-reader/src/app/StripStackWidget.cpp:467` / `h5-reader/src/app/StripStackWidget.cpp:541`: Phase B did not actually collapse the stack to one ordered panel vector; it kept three buckets and always paints temporal tracks, then spectrum tracks, then owned panels. Replace the bucketed storage/painting with a single ordered `panels_` list or an atomic rebuild setter, otherwise Phase D cannot interleave PowerSpectrum/LagDecay panels with the signal order the controller intends.

- [NOW] `h5-reader/src/app/RestServer.cpp:83`: `anchorToJson()` has no `BondVectorAnchor` arm, so `/dashboard/signals` returns `{}` for any iRED/ReorientationalDynamics signal anchored to a bond vector. Add a `bond_vector` JSON kind with `residue` and `kind` fields before the planned REST regressions start asserting the new TRs.

**LATER**

- [LATER] `h5-reader/src/app/DashboardDisplayController.cpp:1498`: status text is still driven only by `activeStripSignalCount_`, so a static-only iRED panel reports "No active strip display modes." Track static panel count separately and report mixed strip/static totals.

- [LATER] `h5-reader/CMakeLists.txt:243`: the new `AbstractStripPanel`/`SequenceBarPanel` files are linked into `h5reader` but not into `h5reader_model_tests`, so the green model test count does not compile or exercise the new app-layer panel path. Add a small Qt widget/controller test target rather than relying on model tests to cover this surface.
