# Rediscover — INDEX (cold-start map)

The current synthesis lives in `NOW.md` + the top of `STATE.md`. The `POSTMORTEM_*` files are
**immutable run-records** — they carry their run-time verdict, which later docs may correct, so read
NOW/STATE for the current truth. (Example: `POSTMORTEM_STAGE2` still says "field = recovered-law"; that
is SUPERSEDED to "field ~null standalone, top combine contributor" by `POSTMORTEM_STAGE2_3` + NOW.md.)

## THE GATE — read this first
The **MATHS AUDIT is NOT done.** The unified combine + every LOAO/between number are **PROVISIONAL**
until it closes. `MATHS_AUDIT_CHECKLIST_2026-06-04.md` is the open agenda. Do NOT quote the unified
0.43/0.26 or any LOAO/between figure as a result yet.

## Minimal reading path (cold start, in order)
1. `NOW.md` — the live marker: where we are, loop ledger, live decisions, the gate.
2. `STATE.md` from the top down to the first `---` divider — the STAGE 2 block + Stage 2.1–2.3 follow-ups
   + the COMBINE NOTE. **Everything below that divider is dated ARCHIVE** (session stratigraphy); read on demand only.
3. `MATHS_AUDIT_CHECKLIST_2026-06-04.md` — the open maths audit (the next session's spine).
4. `analysis/PATTERNS.md` (fitter discipline) + `analysis/FINDINGS.md` (per-result detail) — if touching the fits.
5. `PHYSICS_ARCHITECTURE_UNIFICATION_2026-06-04.md` — the side-frame fold-ins feeding the audit.

Reference-on-demand: `PARTITION_FILTER_DESIGN.md`, the per-stage `POSTMORTEM_*`, the `CODEX_BRIEF_*`.

## Branch / git
`h5-reader-pysr-spike` — NEVER merge / switch / rebase / PR. **The lead owns ALL git**; agents edit/build only.
